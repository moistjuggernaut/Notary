# Dockerfile for running tests
# This builds an image specifically for executing the Python test suite.

# Use the same base image as production for consistency
FROM python:3.12-slim

# Set environment variables
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH /app

# Install system dependencies required for the application
RUN apt-get update && apt-get install -y \
    libgl1-mesa-glx \
    libglib2.0-0 \
    libsm6 \
    libxext6 \
    libxrender-dev \
    libgomp1 \
    libgcc-s1 \
    && rm -rf /var/lib/apt/lists/*

# Set work directory
WORKDIR /app

# Copy requirements first for better caching
COPY requirements.txt .

# Install Python dependencies, including test-specific ones
RUN pip install --no-cache-dir -r requirements.txt && \
    pip install --no-cache-dir pytest pytest-mock

# Copy the entire backend application code and tests
COPY . .

# The command to run when the container starts.
# This will discover and run all tests in the 'tests' directory.
CMD ["pytest", "tests/"] 